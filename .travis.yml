sudo: required
addons:
  chrome: stable

dist: xenial

services:
  - xvfb

language: node_js
node_js:
  - "10.15.3"

branches:
  only:
    - master

before_script:
  - export DISPLAY=:99.0
  - npm install -g @angular/cli

script:
  - ng build --prod --aot
  - ng test
